/**
 * Shared render constants. The font stack references only system fonts — no
 * external font files, which GitHub's Camo proxy would strip. Every card uses
 * the same stack so typography stays consistent.
 */
export const FONT_STACK =
  "-apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif";
